# Ollama Modelfile for the fine-tuned Qwen2.5-3B tool-calling model.
#
# Point FROM at whichever quant you exported (Q4_K_M is the recommended default
# for laptops; Q8_0 for max fidelity). Build with:
#
#     ollama create qwen-tools -f deploy/Modelfile
#     ollama run qwen-tools "What's the weather in Paris in celsius?"
#
# The Streamlit app and the evaluation harness send a fully-rendered prompt with
# raw=True, so the TEMPLATE below only matters for direct `ollama run` chatting.

FROM ../outputs/gguf/model-Q4_K_M.gguf

# Qwen2.5 uses the ChatML format. The system message primes tool usage; the
# model replies with <tool_call>{...}</tool_call> blocks.
SYSTEM """You are a function-calling assistant. Given a user request and a set of
available tools, respond ONLY with the appropriate tool call(s) in the format:
<tool_call>
{"name": "<function_name>", "arguments": {<args>}}
</tool_call>"""

TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>
"""

PARAMETER temperature 0.0
PARAMETER top_p 0.9
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"
PARAMETER num_ctx 4096
